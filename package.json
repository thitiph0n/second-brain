{
  "name": "second-brain-app",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "deploy:frontend": "./scripts/deploy.sh staging",
    "deploy:frontend:staging": "./scripts/deploy.sh staging",
    "deploy:frontend:production": "./scripts/deploy.sh production",
    "deploy:frontend:preview": "wrangler pages deploy apps/web/dist --project-name=second-brain-web-staging --compatibility-date=2025-01-01 --env=preview",
    "deploy:backend": "./scripts/deploy-backend.sh",
    "release": "./scripts/release.sh"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "turbo": "^1.10.0",
    "typescript": "^5.0.0"
  },
  "packageManager": "pnpm@10.12.4"
}
